<span class="span-notice"><%= notice %></span>
<div class="home-page">
          <h1>Welcome to <span class="glyphicon glyphicon-book" aria-hidden="true"></span>Bookers !!</h1>
          <p>In <span class="glyphicon glyphicon-book" aria-hidden="true"></span>Bookers,</p>
          <p>you can share and exchange your opinions , impressions, and emotions</p>
          <p>about various books!</p>

          <% if user_signed_in? %>

          <div class="btn-1"><%= link_to "Log in", user_path(current_user.id), class:'btn btn-default' %></div>
          <div class="btn-2"><%= link_to"Sign up", user_path(current_user.id), class:'btn btn-default' %></div>

          <% else %>
          <div class="btn-1"><%= link_to "Log in", user_session_path, class:'btn btn-default' %></div>
          <div class="btn-2"><%= link_to"Sign up", new_user_registration_path, class:'btn btn-default' %></div>
          <% end %>
</div>













